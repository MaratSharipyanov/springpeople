<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Springpeople. Сотрудники</title>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Springpeople</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/login">Авторизация</a>
                </li>
            </ul>
            <form action="/logout" method="post" class="d-flex">
                <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
                <button class="btn btn-outline-light" type="submit">Выйти</button>
            </form>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <div class="row row-col-2">
        <div class="col-8">
            <h4>Добавить сотрудника</h4>
            <form method="post" action="/main" class="row g-2 mb-4">
                <div class="col-auto">
                    <input type="text" class="form-control" name="name" placeholder="Введите имя">
                </div>
                <div class="col-auto">
                    <input type="text" class="form-control" name="lastname" placeholder="Введите фамилию">
                </div>
                <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
                <div class="col-auto">
                    <button class="btn btn-primary" type="submit">Добавить</button>
                </div>
            </form>

            <h4>Редактировать данные сотрудника</h4>
            <form method="post" action="/update" class="row g-2 mb-4">
                <div class="col-2">
                    <input type="text" class="form-control" name="id" placeholder="Введите id">
                </div>
                <div class="col-auto">
                    <input type="text" class="form-control" name="updname" placeholder="Введите имя">
                </div>
                <div class="col-auto">
                    <input type="text" class="form-control" name="updlastname" placeholder="Введите фамилию">
                </div>
                <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
                <div class="col-auto">
                    <button class="btn btn-primary" type="submit">Обновить</button>
                </div>
            </form>

            <a class="btn btn-primary" data-bs-toggle="collapse" href="#collapseExample" role="button"
               aria-expanded="false" aria-controls="collapseExample">
                Удалить сотрудника
            </a>
            <div class="collapse" id="collapseExample">
                <form method="post" action="/delete" class="row mt-3">
                    <div class="col-2">
                        <input type="text" class="form-control" name="delid" placeholder="Введите id">
                    </div>
                    <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
                    <div class="col-auto">
                        <button class="btn btn-danger" type="submit">Удалить</button>
                    </div>
                </form>
            </div>

            <div class="row mt-5">
                {{#message}}
                    <h5>{{message}}</h5>
                {{/message}}
            </div>
        </div>


        <div class="col-4">
            <h4>Список сотрудников</h4>
            <form method="get" action="/main" class="row g-2 mb-4">
                <div class="col-6">
                    <input type="text" class="form-control" name="filter" value="{{#filter}}{{filter}}{{/filter}}">
                </div>
                <div class="col-auto">
                    <button class="btn btn-primary" type="submit">Найти</button>
                </div>
            </form>

            <div>
                {{#persons}}
                    <div>
                        <b>{{id}}</b>
                        <span>{{name}}</span>
                        <span>{{lastname}}</span>
                    </div>
                {{/persons}}
            </div>
        </div>
    </div>


</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>
</html>